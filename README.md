# Intro_To_AI_3202_Mancala


## Reproducible environment

### Option A — Conda (recommended)
```bash
conda env create -f environment.yml
conda activate mancala-ai
python -m ipykernel install --user --name mancala-ai --display-name "Python (mancala-ai)"
jupyter lab
